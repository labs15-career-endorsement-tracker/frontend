import React from "react"

import "../styles/index"

const Dropdown = props => {
  return (
    <div className="dropdown-container">
      <span>Logout</span>
    </div>
  )
}

export default Dropdown
